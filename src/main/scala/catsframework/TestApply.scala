package catsframework
import cats._ , cats.instances._ , cats.implicits._

object TestApply extends App {

  /*
  val addOne : Int => Int = _ + 1


  Apply[List].ap(List(addOne))(List(1,2,3))

  // another way : List(addOne) ap List(1,2,4)

  Apply[Option].ap(Some(addOne))(Some(4))
  //res2: Option[Int] = Some(5)


  //Fortunately, the Apply type class has variant of the ap function which are ap2 , ap3 until ap22.
  //ap2 means the ap function takes 2 parameters

  (addBoth).some ap2 (3.some , 5.some)



   */

}
